# 🚀 Быстрый старт с Supabase

## Шаг 1: Создайте проект в Supabase

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь завершения (2-3 минуты)

## Шаг 2: Выполните SQL схему

1. В панели Supabase: **SQL Editor** → **New Query**
2. Откройте файл `supabase-schema.sql`
3. Скопируйте **весь код** и вставьте в редактор
4. Нажмите **Run** (или Ctrl+Enter)

✅ Готово! Все таблицы созданы с RLS политиками.

## Шаг 3: Настройте переменные окружения

Создайте файл `frontend/.env`:

```env
REACT_APP_SUPABASE_URL=https://xxxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_anon_key_here
```

**Где найти ключи:**
- Settings → API → Project URL
- Settings → API → anon/public key

## Шаг 4: Установите Supabase клиент

```bash
cd frontend
npm install @supabase/supabase-js
```

## Шаг 5: Используйте в коде

```javascript
import { supabase, getCourses, addToCart } from './services/supabaseClient';

// Получить курсы
const courses = await getCourses({ categoryId: 1 });

// Добавить в корзину
const user = await getCurrentUser();
await addToCart(user.id, courseId);
```

## 🔐 Безопасность

✅ **RLS включен** - каждый пользователь видит только свои данные
✅ **Автоматическая фильтрация** - не нужно проверять права вручную
✅ **Безопасные запросы** - Supabase автоматически применяет политики

## 📊 Что создается автоматически

- ✅ Профиль пользователя при регистрации
- ✅ Курсы в purchased_courses при заказе
- ✅ Обновление updated_at при изменении данных

## 📝 Примеры запросов

См. файл `example-queries.sql` для готовых SQL запросов.

## 🆘 Проблемы?

1. **Ошибка RLS** - проверьте, что пользователь авторизован
2. **Нет данных** - убедитесь, что RLS политики настроены
3. **Ошибка подключения** - проверьте .env файл

---

**Готово!** Теперь ваша база данных настроена и готова к использованию.
